# global-news-website

## Calculator

This repository includes a simple command-line calculator implemented in Python.

Usage:

```
python3 calculator.py <num1> <operator> <num2>
```

Operators supported: `+`, `-`, `*`, `/`.
